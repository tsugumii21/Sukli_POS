import 'package:flutter/material.dart';

class CashierSelectionScreen extends StatelessWidget {
  const CashierSelectionScreen({super.key});
  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: Text('CashierSelectionScreen — Coming soon')));
}
