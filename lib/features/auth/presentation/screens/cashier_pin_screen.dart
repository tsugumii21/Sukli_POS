import 'package:flutter/material.dart';

class CashierPinScreen extends StatelessWidget {
  const CashierPinScreen({super.key});
  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: Text('CashierPinScreen — Coming soon')));
}
