import 'package:flutter/material.dart';

class VoidRefundScreen extends StatelessWidget {
  const VoidRefundScreen({super.key});
  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: Text('VoidRefundScreen — Coming soon')));
}
